# Required for SQL Server and the application connection string.
# Use at least 8 characters with uppercase, lowercase, number, and symbol.
MSSQL_SA_PASSWORD=HotelDesk_Dev2026!

# Optional
DEMO_ENABLED=false
